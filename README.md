# Fitness tracking and statistic Android app

Application use Google Fitness API
Use DataBinding, RxJava, Retrolambda, Prediction API

For deploy own server put key (%filename%.p12) to assets/ directory.
